services:
  app:
    build: .
    image: multi-agent-research:latest
    container_name: multi-agent-research
    ports:
      - "8001:8001"
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
    restart: unless-stopped
